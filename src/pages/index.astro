---
import FeaturedCatalog from '../components/home/FeaturedCatalog.astro';
import FocusAreas from '../components/home/FocusAreas.astro';
import HomeHero from '../components/home/HomeHero.astro';
import HomeOverview from '../components/home/HomeOverview.astro';
import ContentUpdates from '../components/home/ContentUpdates.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { gameTools, projects } from '../data/catalog';
import BaseLayout from '../layouts/BaseLayout.astro';

const websiteSchema = {
	'@context': 'https://schema.org',
	'@type': 'WebSite',
	name: SITE_TITLE,
	url: 'https://269332.xyz',
	description: SITE_DESCRIPTION,
};
---

<BaseLayout title="CloudLight · 项目、游戏工具与文章" description={SITE_DESCRIPTION}>
	<script
		slot="head"
		is:inline
		type="application/ld+json"
		set:html={JSON.stringify(websiteSchema)}
	></script>
	<main class="home">
		<HomeHero />
		<HomeOverview projectsCount={projects.length} gameToolsCount={gameTools.length} />
		<FeaturedCatalog
			id="featured-projects"
			eyebrow="精选项目"
			title="正在维护的项目"
			description="从系统工具到源码补丁，这些项目都来自实际需求。页面中可以查看用途、下载方式和使用说明。"
			items={projects}
			allHref="/projects/"
			allLabel="查看全部项目"
		/>
		<FeaturedCatalog
			id="featured-game-tools"
			eyebrow="游戏工具箱"
			title="游戏相关网页工具"
			description="收录我制作的游戏相关网页工具，包括游戏数据可视化、趣味测试和实用辅助功能。"
			items={gameTools}
			allHref="/tools/"
			allLabel="查看游戏工具箱"
		/>
		<FocusAreas />
		<ContentUpdates />
		<section class="about-callout" aria-labelledby="about-callout-title">
			<div>
				<p class="eyebrow">关于本站</p>
				<h2 id="about-callout-title">了解这个网站</h2>
				<p>这里集中介绍网站收录的项目、游戏工具、文章、使用说明和反馈入口。</p>
			</div>
			<div class="callout-actions">
				<a class="button button-primary" href="/about/">关于本站</a>
				<a
					class="button button-secondary"
					href="https://github.com/yundan125"
					target="_blank"
					rel="noopener noreferrer"
				>
					GitHub <span aria-hidden="true">↗</span>
					<span class="sr-only">（在新窗口打开）</span>
				</a>
			</div>
		</section>
	</main>
</BaseLayout>

<style>
	.home {
		width: min(var(--content-width), calc(100% - 2rem));
		max-width: none;
		padding: 0 0 clamp(4rem, 8vw, 7rem);
	}
	.about-callout {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 2rem;
		margin-top: var(--section-space);
		padding: clamp(1.75rem, 4vw, 2.75rem);
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-large);
		box-shadow: var(--shadow-small);
	}
	.about-callout > div:first-child {
		max-width: 42rem;
	}
	.about-callout p:last-child {
		margin: 0;
		color: var(--color-text-muted);
	}
	.callout-actions {
		display: flex;
		flex: 0 0 auto;
		gap: 0.75rem;
	}
	@media (max-width: 720px) {
		.home {
			width: min(var(--content-width), calc(100% - 1.5rem));
		}
		.about-callout {
			display: block;
		}
		.callout-actions {
			flex-wrap: wrap;
			margin-top: 1.5rem;
		}
	}
	@media (max-width: 420px) {
		.callout-actions .button {
			width: 100%;
		}
	}
</style>
