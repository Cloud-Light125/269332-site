---
import CatalogCard from '../../components/CatalogCard.astro';
import { gameTools } from '../../data/catalog';
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout
	title="游戏工具箱 · CloudLight"
	description="浏览 CloudLight 收录的 Steam 游戏数据可视化、守望先锋趣味测试和其他游戏相关网页工具。"
>
	<main class="catalog-page">
		<header>
			<div>
				<p class="eyebrow">游戏工具</p>
				<h1>游戏工具箱</h1>
				<p>收录我制作的游戏相关网页工具，包括游戏数据可视化、趣味测试和实用辅助功能。</p>
			</div>
			<p class="count"><strong>{gameTools.length}</strong> 个游戏工具</p>
		</header>
		<section class="catalog-grid" aria-label="游戏工具列表">
			{gameTools.map((tool) => <CatalogCard item={tool} />)}
		</section>
		<aside class="copyright-note" aria-label="游戏相关商标与非官方说明">
			<p>《守望先锋》及相关角色、美术素材版权归 Blizzard Entertainment 所有。本站收录的相关工具与 Blizzard Entertainment 无官方关联。</p>
			<p>Steam 是 Valve Corporation 的商标。本站收录的 Steam 数据工具为独立项目，与 Valve Corporation 无官方关联，仅使用账号公开提供的游戏时长信息。</p>
		</aside>
	</main>
</BaseLayout>

<style>
	.catalog-page {
		width: min(var(--content-width), calc(100% - 2rem));
		max-width: none;
	}
	header {
		display: flex;
		align-items: flex-end;
		justify-content: space-between;
		gap: 2rem;
		margin-bottom: 2.5rem;
		padding-bottom: 2rem;
		border-bottom: 1px solid var(--color-border);
	}
	header h1 {
		font-size: clamp(2.25rem, 6vw, 3.75rem);
	}
	header div > p:last-child,
	.count {
		margin-bottom: 0;
		color: var(--color-text-muted);
	}
	.count {
		flex: 0 0 auto;
		font-size: 0.82rem;
	}
	.count strong {
		color: var(--color-text);
		font-size: 1.25rem;
	}
	.catalog-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
	}

	.copyright-note {
		margin-top: 2rem;
		padding: 1rem 1.1rem;
		background: var(--color-surface-muted);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-small);
		color: var(--color-text-muted);
		font-size: 0.78rem;
	}
	.copyright-note p {
		margin: 0;
	}
	.copyright-note p + p {
		margin-top: 0.55rem;
	}
	@media (max-width: 720px) {
		.catalog-page {
			width: min(var(--content-width), calc(100% - 1.5rem));
		}
		header {
			display: block;
		}
		.count {
			margin-top: 1rem;
		}
		.catalog-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
