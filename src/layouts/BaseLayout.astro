---
import type { ImageMetadata } from 'astro';
import BaseHead from '../components/BaseHead.astro';
import CodeBlockEnhancer from '../components/CodeBlockEnhancer.astro';
import SiteFooter from '../components/SiteFooter.astro';
import SiteHeader from '../components/SiteHeader.astro';

interface Props {
	title: string;
	description: string;
	image?: ImageMetadata;
	imageAlt?: string;
	type?: 'website' | 'article';
	noindex?: boolean;
}

const { title, description, image, imageAlt, type = 'website', noindex = false } = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={title} description={description} image={image} imageAlt={imageAlt} type={type} noindex={noindex} />
		<slot name="head" />
	</head>
	<body>
		<a class="skip-link" href="#main-content">跳到主要内容</a>
		<SiteHeader />
		<div id="main-content" tabindex="-1">
			<slot />
		</div>
		<SiteFooter />
		<CodeBlockEnhancer />
	</body>
</html>
