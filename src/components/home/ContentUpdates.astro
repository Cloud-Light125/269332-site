<section class="updates-section" aria-labelledby="updates-title">
	<div>
		<p class="eyebrow">内容更新</p>
		<h2 id="updates-title">这里会持续补充什么</h2>
		<p>
			每个项目不只保留一个源码链接。随着功能变化，这里会逐步补充项目介绍、下载方式、使用步骤、已知限制和更新状态。
		</p>
	</div>
	<ul aria-label="项目页面内容">
		<li>功能介绍</li>
		<li>下载入口</li>
		<li>使用方法</li>
		<li>注意事项</li>
		<li>更新状态</li>
	</ul>
</section>

<style>
	.updates-section {
		display: grid;
		grid-template-columns: minmax(0, 1.35fr) minmax(18rem, 0.65fr);
		align-items: center;
		gap: 3rem;
		margin-top: var(--section-space);
		padding: clamp(2rem, 5vw, 3.25rem);
		background: #152130;
		border-radius: var(--radius-large);
		color: #bdc8d5;
	}
	h2 {
		color: white;
	}
	.updates-section > div > p:last-child {
		margin: 0;
	}
	ul {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}
	li {
		padding: 0.45rem 0.7rem;
		background: rgba(255, 255, 255, 0.07);
		border: 1px solid rgba(255, 255, 255, 0.13);
		border-radius: var(--radius-small);
		color: white;
		font-size: 0.78rem;
		font-weight: 700;
	}
	@media (max-width: 760px) {
		.updates-section {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
	}
</style>
